# regular function
def multiply_by_two(x):
    return x * 2

# Now using a lambda function
multiply_by_two = lambda x: x * 2
multiply_by_two(2)

# 4

# Lambdas can be anonymous functions and do not have to be named
